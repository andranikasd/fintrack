{
  "name": "fintrack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate": "wrangler d1 migrations apply fintrack --remote",
    "db:migrate:local": "wrangler d1 migrations apply fintrack --local",
    "webhook:set": "node scripts/webhook.mjs set",
    "webhook:info": "node scripts/webhook.mjs info",
    "webhook:delete": "node scripts/webhook.mjs delete",
    "build:node": "node scripts/build-node.mjs",
    "start": "node dist/main.mjs",
    "backup": "node dist/backup.mjs",
    "test:docker": "node scripts/smoke-docker.mjs"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "grammy": "^1.30.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260831.1",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0",
    "esbuild": "0.21.5"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
